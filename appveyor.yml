version: 1.0.{build}
image:
- Visual Studio 2019

build:
  verbosity: minimal
  
dotnet_csproj:
  patch: true
  file: '**\*.csproj;**\*.props;**\*.fsproj;**\*.xml'
  
test:
  # only assemblies to test
  assemblies:
    only:
      - '**\*.tests.dll'
